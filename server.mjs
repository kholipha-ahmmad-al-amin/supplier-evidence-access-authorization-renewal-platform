import express from'express';import{join}from'node:path';import{JsonRenewalStore}from'./store.mjs';import{RenewalService}from'./domain.mjs';const store=new JsonRenewalStore(process.env.DATA_FILE||join(process.cwd(),'data','renewals.json')),service=new RenewalService(store.load(),state=>store.save(state)),app=express(),safe=fn=>(req,res)=>{try{fn(req,res)}catch(error){res.status(400).json({error:error.message})}};app.use(express.json());app.get('/health',(_,res)=>res.json({status:'ok',service:'supplier-evidence-access-authorization-renewal-platform'}));app.get('/renewals',(_,res)=>res.json(service.state.renewals));app.post('/renewals',safe((req,res)=>res.status(201).json(service.request(req.body))));app.post('/renewals/:id/assess',safe((req,res)=>res.json(service.assess(req.params.id,req.body.actor,req.body.note))));app.post('/renewals/:id/approve',safe((req,res)=>res.json(service.approve(req.params.id,req.body.actor))));app.post('/renewals/:id/acknowledge',safe((req,res)=>res.json(service.acknowledge(req.params.id,req.body.actor))));app.post('/renewals/:id/verify',safe((req,res)=>res.json(service.verify(req.params.id,req.body.actor))));app.get('/renewals/:id/history',safe((req,res)=>res.json(service.history(req.params.id))));app.listen(process.env.PORT||60000,'0.0.0.0');
